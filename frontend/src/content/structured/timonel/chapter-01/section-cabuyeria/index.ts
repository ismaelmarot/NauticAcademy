import { Section } from '@/content/types';
import { topic as ropesMaterials } from './topic-ropes-materials';
import { topic as knots } from './topic-knots';

export const section: Section = {
  id: 'cabuyeria',
  title: 'Cabuyería',
  topics: [ropesMaterials, knots]
};
